# Django Configuration
DJANGO_SECRET_KEY=your-secret-key-here-change-in-production
DEBUG_MODE=true

# Hosts & CSRF (for local development)
ALLOWED_HOSTS=localhost,127.0.0.1
CSRF_TRUSTED_ORIGINS=http://localhost:8000,http://127.0.0.1:8000

# PostgreSQL Database (Local Development)
DB_NAME=adamsdb
DB_USER=postgres
DB_PASSWORD=your_local_db_password
DB_HOST=localhost
DB_PORT=5432

# MinIO Configuration (Local Development)
# If running MinIO locally, use: localhost:9000
# If using Docker MinIO, use: localhost:9000 (port mapped)
MINIO_URL=localhost:9000
MINIO_ACCESS_KEY=fire_user
MINIO_SECRET_KEY=fire_pasword
MINIO_BUCKET_NAME=adams

# Email Configuration (Optional for local testing)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.hostinger.com
# EMAIL_PORT=465
# EMAIL_USE_SSL=True
# EMAIL_HOST_USER=officedesk@adams.org.in
# EMAIL_HOST_PASSWORD=your_email_password
